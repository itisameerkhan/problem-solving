#include<stdio.h>
#include<string.h>
int palindromeString(char* s)
{
    int l = strlen(s);
    int arr[l] = {0};
    for(int i=0;i<l;i++)
    
}
int main()
{
    char s[] = "carrace";
    if(palindromeString(s)) printf("Palindrome string");
    else printf("Not palindrome string");
}